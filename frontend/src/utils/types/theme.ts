import { CSSProperties } from "@mui/styled-engine";
import { ResponsiveStyleValue, style } from "@mui/system"

export type Theme = {
  background: string,
  topBar: string,
  clock: string,
  icon: string,
  iconDisabled: string,
  iconNotSelected: string,
  boxBackground: string,
  boxBackground2: string,
  boardLight: string,
  boardDark: string,
  signOutButton: string,
  infinity: string,
  negativeIcon: string,
}