import { Box, Divider } from "@mui/material";

export default function ModalSpacer() {
  return <>
      <Box sx={{ height: `20px` }} />
      <Divider variant="middle" />
      <Box sx={{ height: `20px` }} />
    </>;
}